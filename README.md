# CustomAction
This repo is going to practice writing a custom action and then using to improve workflow and productivity.

It allows the user to automatically create a release based upon words in the commits of any branch pushed to the repository. For example, by pushing a commit with the keyword "FIXED", this will trigger the workflow to initiate.
